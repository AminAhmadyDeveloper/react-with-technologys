import React from "react";
import ReactDOM from "react-dom/client";
import ReactRecoil from "./ReactRecoil";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ReactRecoil />
  </React.StrictMode>
);
